# 🏨 호텔 상세 모달/패널 기능 문서

> **담당자**: yongjun  
> **작업 범위**: 호텔 상세 정보 표시 (모달/패널 형태)  
> **스타일 참고**: 네이버 호텔

---

## 📁 프로젝트 구조

```
src/
├── components/
│   └── hotel/
│       ├── HotelDetail.js           # 호텔 상세 정보 메인 컴포넌트
│       ├── HotelDetailPanel.js      # 패널 래퍼 컴포넌트 (네이버 스타일)
│       ├── HotelGallery.js          # 이미지 갤러리
│       ├── HotelInfo.js             # 호텔 기본 정보
│       ├── HotelAmenities.js        # 편의시설
│       ├── HotelReviews.js          # 리뷰
│       ├── HotelLocation.js         # 위치 정보
│       ├── HotelPolicy.js           # 이용 정책
│       ├── RoomCard.js              # 객실 카드
│       ├── LiveViewerCount.js       # 실시간 조회수
│       ├── mockHotelData.js         # 임시 데이터 (API 연동 전)
│       └── index.js                 # Export 통합
│
└── app/
    ├── hotel-search/
    │   └── page.js                  # 호텔 검색 결과 페이지 (패널 사용)
    └── hotel/
        └── [id]/
            └── page.js              # 호텔 상세 전체 페이지

```

---

## 🎯 주요 기능

### 1️⃣ 호텔 상세 정보 표시 (2가지 모드)

#### **패널 모드** (`isModal={true}`)

- **위치**: `src/app/hotel-search/page.js`
- **사용 컴포넌트**: `HotelDetailPanel`
- **특징**:
  - 호텔 리스트 오른쪽에 555px 폭의 패널로 표시
  - 배경 오버레이 없음 (PC에서만, 모바일은 있음)
  - 호텔 리스트와 지도가 그대로 보임
  - 다른 호텔 클릭 가능
  - ESC 키로 닫기 가능

#### **전체 페이지 모드** (`isModal={false}`)

- **위치**: `src/app/hotel/[contentId]/page.js`
- **사용 컴포넌트**: `HotelDetail` (직접)
- **특징**:
  - 전체 페이지로 호텔 정보 표시
  - Header, Footer 포함
  - SEO 최적화 (메타데이터, 구조화된 데이터)
  - URL: `/hotel/[호텔ID]`

---

## 🧩 컴포넌트 상세

### 📄 HotelDetail.js (메인 컴포넌트)

**역할**: 호텔 상세 정보 전체 UI 및 로직 관리

**주요 기능**:

1. **Sticky 헤더**

   - 호텔명, 별점, 가격 정보 표시
   - 스크롤 시 항상 상단에 고정
   - 패널: 패널 상단에 고정
   - 전체 페이지: 메인 Header 바로 아래 고정

2. **Sticky 네비게이션**

   - 섹션 목록: 객실, 편의시설, 리뷰, 위치, 정책
   - 헤더 바로 아래에 동적으로 고정
   - 현재 보이는 섹션 자동 하이라이트
   - 클릭 시 해당 섹션으로 스크롤

3. **섹션 관리**

   - `sectionsRef`로 각 섹션 위치 추적
   - 스크롤 감지 → 현재 섹션 업데이트
   - requestAnimationFrame으로 성능 최적화

4. **동적 높이 계산**
   - 헤더 높이 자동 측정 (`headerHeight` state)
   - 패널: HotelDetail 헤더만
   - 전체 페이지: 메인 Header + HotelDetail 헤더
   - 리사이즈 대응

**Props**:

```javascript
{
  contentId : number|string,        // 호텔 ID
  searchParams: Object,          // 검색 조건 (체크인/아웃, 인원 등)
  isModal: boolean,              // 패널 모드 여부
  scrollContainerRef: RefObject  // 외부 스크롤 컨테이너 (패널용)
}
```

**주요 State**:

- `activeSection`: 현재 활성 섹션
- `isScrollingToSection`: 섹션 클릭 스크롤 중 여부
- `headerHeight`: 동적으로 측정된 헤더 높이

---

### 📄 HotelDetailPanel.js

**역할**: 네이버 호텔 스타일 패널 래퍼

**주요 기능**:

1. ESC 키로 패널 닫기
2. 배경 오버레이 (모바일만)
3. 슬라이드 인 애니메이션
4. 패널 내 스크롤 관리

**레이아웃**:

```
PC:
┌─────────┬────────────┬──────────┐
│ 리스트   │   패널     │   지도    │
│ (30%)   │ (555px)   │ (나머지)  │
└─────────┴────────────┴──────────┘

모바일:
┌──────────────┐
│ 전체 화면    │
│ 패널 (100%) │
└──────────────┘
```

**CSS 클래스**:

- `lg:left-[calc(30%+1rem)]` - 리스트 바로 오른쪽
- `lg:w-[555px]` - 고정 폭
- `lg:top-[120px]` - 상단 여백
- `lg:h-[calc(100vh-140px)]` - 하단 여백
- `lg:rounded-xl` - 둥근 모서리

---

### 📄 서브 컴포넌트들

#### HotelGallery.js

- 호텔 이미지 갤러리 (메인 + 서브 이미지)
- 2x2 그리드 레이아웃
- 호버 효과, 이미지 개수 표시

#### HotelInfo.js

- 호텔 기본 정보 (설명, 체크인/아웃 시간, 별점)
- 깔끔한 카드 형태

#### HotelAmenities.js

- 편의시설 목록
- 3컬럼 그리드 레이아웃
- 체크마크 아이콘

#### HotelReviews.js

- 고객 리뷰 목록
- 평점, 작성자, 날짜, 객실 타입 표시
- 호버 효과

#### HotelLocation.js

- 호텔 주소 및 지도 영역
- TODO: 네이버/카카오 지도 API 연동

#### HotelPolicy.js

- 체크인/아웃, 취소, 아동, 반려동물 정책
- 섹션별로 정리된 레이아웃

#### RoomCard.js

- 객실 정보 카드
- 가격, 할인율, 편의시설, 예약 버튼
- 호버 시 확대 효과

#### LiveViewerCount.js

- 실시간 조회수 표시
- WebSocket 연동 준비 (현재 랜덤)
- 애니메이션 효과

#### mockHotelData.js

- 개발용 임시 데이터
- 실제 API 연동 전까지 사용
- TODO: API 연동 후 제거 예정

---

## 🎨 스타일링

### Sticky 동작 원리

```javascript
// 1. 헤더 높이 동적 측정
const [headerHeight, setHeaderHeight] = useState(0);

useEffect(() => {
  const updateHeaderHeight = () => {
    let totalHeight = headerRef.current.offsetHeight;

    // 전체 페이지: 메인 Header 높이 추가
    if (!isModal) {
      const mainHeader = document.querySelector("header");
      if (mainHeader) {
        totalHeight += mainHeader.offsetHeight;
      }
    }

    setHeaderHeight(totalHeight);
  };

  updateHeaderHeight();
  window.addEventListener("resize", updateHeaderHeight);
}, [isModal]);

// 2. 네비게이션을 측정된 높이만큼 아래에 고정
<div style={{ top: `${headerHeight}px` }} className="sticky">
  {/* 네비게이션 */}
</div>;
```

### 섹션 감지 로직

```javascript
// 1. 각 섹션의 위치 추적
const sectionsRef = useRef({});
<div ref={(el) => sectionsRef.current['rooms'] = el}>

// 2. 스크롤 시 현재 섹션 계산
const scrollY = isModal ? scrollElement.scrollTop : window.scrollY;
const threshold = headerHeight + navHeight + 10;

Object.entries(sectionsRef.current).forEach(([key, element]) => {
  const distance = Math.abs(scrollY + threshold - element.offsetTop);
  if (distance < closestDistance) {
    currentSection = key;
  }
});

setActiveSection(currentSection);
```

### 반응형 디자인

```css
/* 패널 */
.lg:left-[calc(30%+1rem)]  /* 리스트 오른쪽 */
.lg:w-[555px]              /* 고정 폭 */
.lg:top-[120px]            /* 상단 여백 */
.lg:rounded-xl             /* 둥근 모서리 */

/* 전체 화면 (모바일) */
.w-full
.h-full
```

---

## 🔄 데이터 흐름

```
1. 호텔 클릭
   ↓
2. hotel-search/page.js
   - setSelectedcontentId (contentId )
   ↓
3. HotelDetailPanel 렌더링
   - contentId , searchParams 전달
   ↓
4. HotelDetail 렌더링
   - getMockHotelData(contentId ) 호출
   - 섹션별 컴포넌트 렌더링
   ↓
5. 사용자 인터랙션
   - 네비게이션 클릭 → 섹션 스크롤
   - 스크롤 → 현재 섹션 업데이트
   - ESC/X 버튼 → 패널 닫기
```

---

## 🚀 향후 개선 사항

### 단기 (1-2주)

- [ ] 실제 API 연동 (mockHotelData.js 제거)
- [ ] 지도 API 연동 (네이버/카카오)
- [ ] 이미지 갤러리 슬라이더 구현
- [ ] 실시간 조회수 WebSocket 연동
- [ ] 예약 버튼 기능 구현

### 중기 (1개월)

- [ ] 이미지 레이지 로딩
- [ ] 리뷰 페이지네이션
- [ ] 객실 상세 모달
- [ ] 즐겨찾기 기능
- [ ] 공유하기 기능

### 장기 (2개월+)

- [ ] 360도 VR 투어
- [ ] AI 챗봇 문의
- [ ] 다국어 지원
- [ ] 접근성 개선 (WCAG 2.1)

---

## 🐛 알려진 이슈

### 해결됨 ✅

- ~~패널이 전체 화면을 덮는 문제~~ → 배경 오버레이 PC에서 제거
- ~~네비게이션이 웹페이지 상단으로 올라가는 문제~~ → sticky 수정
- ~~헤더/네비게이션 잘림 문제~~ → 동적 높이 계산 추가
- ~~깜박임 문제~~ → requestAnimationFrame 사용

### 진행 중 🔄

- 없음

### 추후 수정 예정 📝

- 이미지 placeholder 실제 이미지로 교체
- 리뷰 더보기 기능
- 모바일 최적화

---

## 📊 성능 최적화

### 적용된 최적화

1. **requestAnimationFrame**: 스크롤 이벤트 최적화
2. **passive: true**: 스크롤 리스너 성능 향상
3. **컴포넌트 분리**: 코드 스플리팅 가능
4. **useRef**: 불필요한 리렌더링 방지

### 측정 지표 (목표)

- FCP (First Contentful Paint): < 1.5s
- LCP (Largest Contentful Paint): < 2.5s
- CLS (Cumulative Layout Shift): < 0.1
- TTI (Time to Interactive): < 3.5s

---

## 🔐 보안 고려사항

1. **XSS 방지**: React의 기본 이스케이핑 사용
2. **CSRF 방지**: API 연동 시 토큰 필요
3. **민감 정보**: 가격/예약 정보는 서버에서 검증
4. **Rate Limiting**: 조회수 API 과도한 요청 방지

---

## 📖 참고 자료

- [네이버 호텔](https://hotels.naver.com/) - UI/UX 참고
- [Next.js 공식 문서](https://nextjs.org/docs)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [프로젝트 규칙](/.cursor/rules/yongjun-hotel.mdc)

---

## 👥 협업 규칙

### ⚠️ 절대 하지 말 것

- ❌ `junhyeong`의 검색/필터/리스트 코드 수정
- ❌ 공통 데이터 구조 변경
- ❌ 다른 팀원의 함수명/변수명 변경
- ❌ 다른 팀원의 주석/TODO 삭제

### ✅ 해야 할 것

- ✅ 자신의 모달/패널 기능만 개선
- ✅ 공통 인스턴스(axios, zustand) 참조만
- ✅ API 호출 시 try-catch 필수
- ✅ 오류 발생 시 UI 피드백 (alert/toast)
- ✅ JSDoc 주석 작성
- ✅ 300줄 이상 시 컴포넌트 분리

### 📝 코드 리뷰 요청 시

1. 자신의 영역만 변경했는지 확인
2. 린트 에러 없는지 확인
3. 다른 기능에 영향 없는지 테스트
4. 변경 사항 명확히 기록

---

## 📞 문의

- **담당자**: yongjun
- **영역**: 호텔 상세 모달/패널
- **충돌 시**: PR/코멘트로 전달

---
_Last Updated: 2024-10-16_
