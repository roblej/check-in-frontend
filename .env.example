# ============================================
# 프론트엔드 환경 변수 설정 예시 파일
# ============================================
# 사용 방법:
# 1. 이 파일을 .env.local로 복사: cp .env.example .env.local
# 2. 실제 값으로 채워넣기
# 3. .env* 파일은 Git에 커밋하지 않도록 주의 (.gitignore에 포함됨)
# ============================================
# 참고: NEXT_PUBLIC_ 접두사가 붙은 변수는 클라이언트 사이드에서 접근 가능합니다.
#       민감한 정보는 NEXT_PUBLIC_ 접두사를 사용하지 마세요.
# ============================================

# ============================================
# API 설정
# ============================================
# 백엔드 API URL (프로덕션: https://api.checkinn.store 또는 EC2 IP)
NEXT_PUBLIC_API_URL=http://localhost:8888

# 서버 사이드에서 백엔드로 직접 요청 시 사용 (프로덕션: EC2 직접 IP)
BACKEND_HOST=http://localhost:8888

# ============================================
# Toss Payments 설정
# ============================================
# Toss Payments 클라이언트 키 (클라이언트에서 사용, 공개 가능)
NEXT_PUBLIC_TOSS_CLIENT_KEY=your_toss_client_key

# Toss Payments 시크릿 키 (서버 사이드에서만 사용, 절대 공개하지 마세요)
TOSS_SECRET_KEY=your_toss_secret_key

# ============================================
# 카카오 맵 API 설정
# ============================================
# 카카오 맵 JavaScript API 키
NEXT_PUBLIC_KAKAO_MAP_API_KEY=your_kakao_map_api_key

# ============================================
# 한국관광공사 Tour API 설정
# ============================================
# 한국관광공사 Tour API 서비스 키
NEXT_TOUR_KEY=your_tour_api_key

# ============================================
# 이미지 URL 설정
# ============================================
# 객실 이미지 베이스 URL (S3 버킷 URL)
NEXT_PUBLIC_ROOM_IMAGE_BASE_URL=https://your-s3-bucket.s3.ap-northeast-2.amazonaws.com/room/

# ============================================
# HTTPS 리다이렉트 설정
# ============================================
# 프로덕션 환경에서 HTTP를 HTTPS로 리다이렉트할지 여부
# 로컬 개발: false, 프로덕션: true
ENABLE_HTTPS_REDIRECT=false
